<center>
<h1>Routes</h1>

<% @routes.each do |route| %>
  <h3><%= link_to route.name, route_path(route) %></h3>
    <p>Location: <%= link_to route.location.name, location_path(route.location) %><br><br></p>
<% end %>

<%= button_to "Create New Route", new_route_path, method: "get" %>
</center>