<!DOCTYPE html>
<html>
<body>

<%= render partial: "partials/navbar" %> 
  
<div class="community-body">
  <h1>Other Members in the <%=@community.name%> Community:</h1>
  <%@community.climbers.each do |climber|%> 
    <li><%=link_to climber.name, climber_path(climber)%></li>
    <%end%>
</div>
</html>
</body>